/**
 * @format
 */

import {AppRegistry} from 'react-native';
import WalletComponent from './components/WalletComponent';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WalletComponent);

